# llm_tamp
TAMP with common sense from LLM

# Installation
```
$ git clone --recursive <repository-url>
$ cd llm_tamp
$ git submodule update --init --recursive
$ cd pddlstream
$ ./downward/build.py
```
